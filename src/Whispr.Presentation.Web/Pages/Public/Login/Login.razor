@page "/login"

@using Whispr.Presentation.Web.Components.Features.GoogleButton

<div class="min-vh-100 d-flex align-items-center justify-content-center bg-light">
    <div class="card shadow-sm p-4" style="width: 100%; max-width: 380px;">

        <h4 class="mb-4 text-center fw-semibold">Entrar</h4>

        <div class="mb-3">
            <label class="form-label">Email</label>
            <input type="email" class="form-control" placeholder="seu@email.com"
                   @bind="Email" />
        </div>

        <div class="mb-4">
            <label class="form-label">Senha</label>
            <input type="password" class="form-control" placeholder="••••••••"
                   @bind="Password" />
        </div>

        <button class="btn btn-primary w-100 mb-2" @onclick="HandleLogin">
            Entrar
        </button>

        <hr class="my-3 text-muted" />

        <GoogleButton OnLoginSuccess="HandleGoogleLogin" />

    </div>
</div>